spring.datasource.url=${TEST_DB_URL:jdbc:postgresql://localhost:5432/learning_stack_test}
spring.datasource.username=${TEST_DB_USERNAME:learning_stack}
spring.datasource.password=${TEST_DB_PASSWORD:learning_stack}
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.clean-disabled=false
